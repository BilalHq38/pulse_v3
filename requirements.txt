# Pulse Engine - PostgreSQL edition
# Install: pip install -r requirements.txt

# Web framework (keep pydantic/starlette aligned for pip's resolver in slim images)
fastapi>=0.115.0,<0.137
uvicorn[standard]>=0.29.0,<0.35
pydantic>=2.10.0,<2.14.0

# PostgreSQL (asyncpg replaces motor/pymongo entirely)
asyncpg>=0.29.0
SQLAlchemy>=2.0.44,<2.1
alembic>=1.16.5,<1.17
psycopg[binary]>=3.2.13,<3.3
greenlet>=3.4.0,<4

# WebSockets / real-time
python-socketio>=5.11.0

# HTTP client (OAuth flows, AI API calls)
httpx>=0.27.0
google-genai>=1.11.0
openai>=1.68.0
anthropic>=0.49.0
redis>=5.0.0

# Auth
python-jose[cryptography]>=3.3.0
cryptography>=43.0.0
passlib[bcrypt]>=1.7.4
stripe>=11.0.0

# File uploads / cloud storage
python-multipart>=0.0.9
aiofiles>=23.2.1
boto3>=1.34.0

# Office file handling (onboarding doc uploads)
Pillow>=10.0.0
openpyxl>=3.1.0

# Tabular imports (shared/tabular_uploads.py, customer bulk upload routes)
numpy>=1.26.0
pandas>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# Phone E.164 validation (must match backend/core/phone_normalization.py; keep in sync with backend/requirements.txt)
phonenumbers>=8.13.0,<9

# Lightweight local ML model: all-MiniLM-L6-v2 via ONNX (sentiment, intent, buying signals, lead quality)
fastembed>=0.3.6
